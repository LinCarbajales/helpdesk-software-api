spring.application.name=helpdesk-software-api

# Core
spring.profiles.active=devh2

#base end-point config
api-endpoint = api/v1

#initial data
spring.jpa.hibernate.ddl-auto=create
spring.jpa.defer-datasource-initialization=true